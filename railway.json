{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "npm install && npm run build:shared && npm run build:web && npm run build -w @acuo/server"
  },
  "deploy": {
    "startCommand": "echo 'Use per-service configs in apps/web/railway.json and apps/server/railway.json'",
    "restartPolicyType": "ON_FAILURE"
  }
}
